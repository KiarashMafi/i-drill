# 🚀 راهنمای اجرای داشبورد i-Drill

## 📋 خلاصه

برای اجرای داشبورد، باید **دو سرور** را همزمان اجرا کنید:
1. **بک‌اند (Backend API)** - روی پورت 8001
2. **فرانت‌اند (Dashboard)** - روی پورت 3000

---

## 🎯 روش سریع: دو پنجره PowerShell

### پنجره 1: بک‌اند

یک پنجره PowerShell جدید باز کنید و دستورات زیر را اجرا کنید:

```powershell
cd "C:\Users\asus\Documents\companies\ithub\AI\products\clones\i drill\i-drill\src\backend"
python start_server.py
```

**یا:**

```powershell
cd "C:\Users\asus\Documents\companies\ithub\AI\products\clones\i drill\i-drill\src\backend"
python -m uvicorn app:app --host 0.0.0.0 --port 8001 --reload
```

**انتظار:** باید پیام مشابه این را ببینید:
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8001
```

---

### پنجره 2: فرانت‌اند (داشبورد)

یک پنجره PowerShell **جدید دیگر** باز کنید و دستورات زیر را اجرا کنید:

```powershell
cd "C:\Users\asus\Documents\companies\ithub\AI\products\clones\i drill\i-drill\frontend"
npm run dev
```

**انتظار:** باید پیام مشابه این را ببینید:
```
  VITE v5.0.8  ready in XXX ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

---

## 🌐 دسترسی به داشبورد

پس از اجرای هر دو سرور:

1. **داشبورد:** http://localhost:3000 را در مرورگر باز کنید
2. **API Documentation:** http://localhost:8001/docs را برای مشاهده API ها باز کنید

---

## ✅ بررسی وضعیت سرورها

برای اطمینان از اجرای سرورها، در یک پنجره PowerShell دیگر این دستور را اجرا کنید:

```powershell
# بررسی پورت 8001 (بک‌اند)
netstat -ano | findstr :8001

# بررسی پورت 3000 (فرانت‌اند)
netstat -ano | findstr :3000
```

اگر هر دو پورت `LISTENING` هستند، یعنی سرورها در حال اجرا هستند.

---

## 🔧 تست اتصال بک‌اند

برای اطمینان از کارکرد بک‌اند:

```powershell
Invoke-WebRequest -Uri http://localhost:8001/api/v1/health -Method GET
```

اگر پاسخ موفقیت‌آمیز بود، بک‌اند درست کار می‌کند.

---

## ⚠️ مشکلات احتمالی

### بک‌اند اجرا نمی‌شود:
- بررسی کنید که Python نصب است: `python --version`
- بررسی کنید که وابستگی‌ها نصب شده‌اند: `pip list | findstr fastapi`
- خطاها را در خروجی ترمینال بررسی کنید

### فرانت‌اند اجرا نمی‌شود:
- بررسی کنید که Node.js نصب است: `node --version`
- بررسی کنید که npm packages نصب شده‌اند: `cd frontend && npm list`
- پورت 3000 را بررسی کنید که اشغال نباشد

### داشبورد به بک‌اند متصل نمی‌شود:
- مطمئن شوید بک‌اند روی پورت 8001 در حال اجرا است
- بررسی کنید که `frontend/vite.config.ts` به پورت 8001 اشاره می‌کند
- Console مرورگر را باز کنید (F12) و خطاها را بررسی کنید

---

## 🛑 توقف سرورها

برای توقف سرورها:

1. در هر پنجره PowerShell، دکمه‌های **Ctrl + C** را بزنید
2. یا پنجره‌های PowerShell را ببندید

---

## 📝 نکات مهم

- **هر دو سرور باید همزمان اجرا شوند**
- **پنجره‌های PowerShell را نبندید** (سرورها در آن‌ها اجرا می‌شوند)
- **برای تغییرات کد:** 
  - بک‌اند با `--reload` خودکار restart می‌شود
  - فرانت‌اند با Vite خودکار reload می‌شود

---

## 🎉 پس از اجرا

شما باید بتوانید:
- ✅ داشبورد را در http://localhost:3000 ببینید
- ✅ API ها را در http://localhost:8001/docs تست کنید
- ✅ داده‌ها را در داشبورد مشاهده کنید (اگر بک‌اند درست کار کند)

---

**تاریخ:** 2025-01-15

