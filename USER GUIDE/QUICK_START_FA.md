# ⚡ راه‌اندازی سریع داشبورد i-Drill

## 🎯 روش سریع (توصیه می‌شود)

### استفاده از اسکریپت PowerShell:

```powershell
.\start_dashboard.ps1
```

این اسکریپت:
- ✅ بک‌اند را در یک پنجره PowerShell اجرا می‌کند
- ✅ فرانت‌اند را در یک پنجره PowerShell دیگر اجرا می‌کند
- ✅ آدرس‌ها را نمایش می‌دهد

---

## 🌐 دسترسی به داشبورد

پس از اجرای اسکریپت:

1. **داشبورد:** http://localhost:3000
2. **API Documentation:** http://localhost:8001/docs

---

## 📝 روش دستی (اگر اسکریپت کار نکرد)

### گام 1: بک‌اند

یک پنجره PowerShell باز کنید:

```powershell
cd src\backend
python start_server.py
```

### گام 2: فرانت‌اند

یک پنجره PowerShell جدید باز کنید:

```powershell
cd frontend
npm run dev
```

---

## ✅ بررسی وضعیت

برای اطمینان از اجرای سرورها:

```powershell
# بررسی بک‌اند
netstat -ano | findstr :8001

# بررسی فرانت‌اند
netstat -ano | findstr :3000
```

اگر هر دو `LISTENING` هستند، یعنی سرورها در حال اجرا هستند.

---

## 🛑 توقف سرورها

- در هر پنجره PowerShell: **Ctrl + C**
- یا پنجره‌های PowerShell را ببندید

---

## ⚠️ مشکلات احتمالی

### خطای "python not found"
- مطمئن شوید Python نصب است: `python --version`

### خطای "npm not found"
- مطمئن شوید Node.js نصب است: `node --version`

### پورت اشغال است
- بررسی کنید که هیچ برنامه دیگری از پورت‌ها استفاده نمی‌کند
- یا پورت را در تنظیمات تغییر دهید

---

**تاریخ:** 2025-01-15

